#include<stdio.h>
#include<stdlib.h>

#define N 50 /*N= nombre de somet */



typedef struct arb{
    unsigned int key;
    struct node *left; //gauche 
    struct node *right; // droite
}arb;

void ajouterarb(arb **tree, unsigned int key){
    arb *auxnode;
    arb *tmparb = *tree;

    arb *elem = (abr *) malloc (1*sizeof(abr));
    elem->key = key;
    elem->left = NULL;
    elem->right = NULL;

    if(tmparb)
        do {
            tmparb =
        }

}


main(){
    
}
